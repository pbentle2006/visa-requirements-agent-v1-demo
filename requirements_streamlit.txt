# Streamlit Cloud Optimized Requirements
# Core dependencies for V1 Demo (no OpenAI required)
streamlit>=1.30.0
pandas>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
pydantic>=2.0.0
plotly>=5.17.0

# Document parsing (lightweight versions)
PyPDF2>=3.0.0
python-docx>=0.8.11
openpyxl>=3.1.0

# Optional: Remove if deployment size is an issue
# jupyter>=1.0.0
# pytest>=7.4.0
# black>=23.0.0
